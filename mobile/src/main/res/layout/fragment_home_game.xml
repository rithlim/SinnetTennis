<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/home_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:gravity="center"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="top"
        android:orientation="horizontal"
        android:paddingTop="10dp">

        <TextView
            android:id="@+id/homePlayer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:paddingLeft="@dimen/player_name_label_padding"
            android:text="@string/player_one_label"
            android:textColor="@color/home_game_score_label_color"
            android:textSize="@dimen/player_name_label_size"
            android:textStyle="bold"
            android:visibility="visible" />

        <EditText
            android:id="@+id/homePlayerEditText"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_gravity="center"
            android:enabled="false"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:hint="@string/player_name_hint"
            android:maxLength="10"
            android:singleLine="true"
            android:textSize="@dimen/player_name_edit_text_size"
            android:visibility="gone" />

    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/home_score"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:paddingTop="@dimen/game_score_top_padding"
            android:shadowColor="@color/black"
            android:shadowDx="3"
            android:shadowDy="3"
            android:shadowRadius="6"
            android:text="45"
            android:textColor="@color/home_game_score_label_color"
            android:textSize="@dimen/game_score_label_normal_size" />

        <ImageView
            android:id="@+id/server_icon"
            android:layout_width="@dimen/server_icon_width"
            android:layout_height="@dimen/server_icon_width"
            android:src="@drawable/racket72"
            android:visibility="invisible" />

        <TextView
            android:id="@+id/home_server_receiver"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/server_label"
            android:textColor="@color/server_receiver_label_color"
            android:textSize="@dimen/server_receiver_label_size"
            android:textStyle="bold" />
    </LinearLayout>
</LinearLayout>

